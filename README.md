Instruction
==============

Install it
--------
Clone folder and then run:

```shell
npm install
```

Start
--------
In folder run
```
npm run devstart
```

Use it
--------
Open browser to:
```
http://localhost:3000/login
```